# 项目开发过程中使用的提示词清单

## 文档信息

| 项目 | 内容 |
| :--- | :--- |
| **文档版本** | V1.0 |
| **创建日期** | 2026-05-10 |
| **所属项目** | QA Live Healthcare Interview |

---

## 提示词清单

### 阶段1：项目环境检查与启动

**提示词1:**
```
请检查我的vue运行环境，并运行当前的web项目
```

**提示词2:**
```
请用谷歌浏览器打开http://localhost:5173/ 查看效果
```

---

### 阶段2：多语言支持功能

**提示词3:**
```
给web端的首页添加中英文切换能力：
1. 在页面右上角添加语言切换下拉菜单，在用户选择 中文/English 选项时动态切换页面显示内容到对应语言。
2. 中英文语言资源文件需要保存在 web/qa-web/src/locales 目录中。
3. 只需要处理首页本身，无需处理其他页面。
```

**提示词4:**
```
首页右上角语言选中"英文"切换其他tab页面，我希望从其他页面返回首页，首页的语言状态还是"英文"。即：首页要记录当前选中的语言状态。
```

---

### 阶段3：数据库环境配置

**提示词5:**
```
检查docker环境
```

**提示词6:**
```
当前应用的数据全部使用json文件的方式在前端项目中存储，请将医生相关的数据使用mysql数据库存储。具体要求如下：
1. 创建 docker-compose.yml 用于创建mysql数据库，并挂接phpmyadmin作为数据库服务器管理界面
2. 在 qa-service-user 服务中创建可以支持前端 医生页面 显示医生列表所需要的api
3. 完成前后端联调
```

**提示词7:**
```
继续
```

**提示词8:**
```
mysql数据库doctors表 中文是乱码
```

**提示词9:**
```
启动前端服务
```

---

### 阶段4：用户认证功能开发

**提示词10:**
```
我现在有个需求：为问诊用户提供用户名密码登录方式，首先在 /docs 目录中编写规范的PRD文档模版。
```

**提示词11:**
```
在问诊页面 Consultation.vue "患者身份验证"标签右侧添加"密码登录"切换按钮，点击按钮实现prd文档的功能
```

**提示词12:**
```
在mysql中创建对应的表，并在后端实现相应的功能联调。
```

---

### 阶段5：API测试文档

**提示词13:**
```
使用 curl 构建对于以上需求中所涉及的所有接口的测试,提供 /docs/API_TEST.md 用于描述以上接口测试流程,在单独的文件生成测试报告
```

**提示词14:**
```
/docs/prompts.md 中列出在这个过程中你所使用的所有提示词
```

---

## 提示词分类统计

| 类别 | 数量 | 说明 |
|---|---|---|
| 环境检查与启动 | 3 | Vue环境检查、浏览器打开、Docker检查 |
| 多语言功能 | 2 | 中英文切换、语言状态保持 |
| 数据库配置 | 5 | MySQL配置、API开发、中文乱码修复 |
| 用户认证功能 | 3 | PRD文档、登录功能实现、后端联调 |
| 测试文档 | 2 | API测试文档、提示词清单 |
| **总计** | **14** | 所有用户提示词 |

---

## 关键技术实现对应提示词

| 提示词 | 实现内容 | 关键文件 |
|---|---|---|
| 中英文切换能力 | 首页多语言切换下拉菜单 | `Home.vue`, `locales/zh.ts`, `locales/en.ts`, `store/index.ts` |
| 语言状态保持 | 使用store管理全局语言状态 | `store/index.ts` |
| MySQL数据库存储 | 医生数据迁移到MySQL | `docker-compose.yml`, `init-scripts/init.sql`, `DoctorController.java` |
| 中文乱码修复 | MySQL字符集配置 | `docker-compose.yml`, `init-scripts/init.sql` |
| PRD文档编写 | 用户登录功能PRD | `docs/PRD_User_Login.md` |
| 密码登录功能 | 问诊页面登录切换 | `Consultation.vue` |
| 后端联调 | 患者API接口 | `Patient.java`, `PatientRepository.java`, `PatientController.java` |
| API测试文档 | 接口测试用例和报告 | `docs/API_TEST.md`, `docs/test-reports/api_test_report.md` |

---

**文档结束**
